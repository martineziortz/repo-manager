name: '[Build] Merge Dependabot PRs'

on: pull_request_target

permissions:
  pull-requests: write
  contents: write

jobs:
  dependabot:
    uses: dbd-net/misc-repo-manager/.github/workflows/dependabot-auto-merge.yml@develop
    secrets: inherit
