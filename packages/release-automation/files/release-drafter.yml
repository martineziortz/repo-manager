name: Release Drafter

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  draft-new-release:
    uses: dbd-net/misc-repo-manager/.github/workflows/shared-release-drafter.yml@develop
    secrets: inherit
